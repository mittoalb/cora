Preconditions for `start_run`: intake routines (`beamtime_intake`, `mount_sample`) that register a Subject and mount it on the apparatus, and gate checks (`proposal_clearance`, `run_start_gated_by_clearance`) that prove an Active Clearance is in place before any acquisition.
