Operator-started Plan executions from `start_run` through a terminal state to Dataset registration. Acquisition modes (`tomography_scan`, `continuous_rotation_sweep`, `mosaic_acquisition`, `streaming_tomography`, `data_publish`, `energy_change`, `run_reading_logbook`) and lifecycle edges (`run_hold_resume_cycle`, `run_stopped_early`, `run_truncated_after_outage`, `dismount_sample`).
