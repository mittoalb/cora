Output written by an AI agent that subscribes to domain events and emits an advisory `DecisionRegistered` per Run. Today that is `RunDebrief` on each terminal Run (`run_debrief` for Nominal, `run_debrief_degraded` for Degraded, `run_debrief_aborted` for EquipmentAbort). Advisory means non-binding: operators can override it, rate it via `DecisionRated`, and a Run is never blocked by it.
