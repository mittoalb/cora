"""Aggregates owned by the Agent BC.

One aggregate: `Agent`. A sibling `PromptTemplate` aggregate is
deferred per [[project_agent_bc_research]] watch item #2 until the
rule-of-three trigger fires.
"""
