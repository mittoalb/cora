"""Aggregates owned by the Supply BC.

One aggregate: `Supply` (continuously-available resource; multiple
instances at runtime, one per resource: photon beam, LN2, compressed
air, electrical power, cooling water, vacuum, process gases, compute
pool). Per the BC map, Supply is the Track B intro aggregate and the
only aggregate in this BC for the foreseeable future.
"""
