"""Aggregates owned by the Equipment BC.

Phase 5a ships `Capability` (technique-class catalog). Phase 5b
will add `Asset` (physical equipment instance with hierarchy +
lifecycle); the two coexist in this BC because Recipe.Method
references `Capability` and Recipe.Plan references `Asset`, and
both are Foundation-tier shared concepts (per the BC map).
"""
