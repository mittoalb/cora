"""Vertical slices for the Access BC.

One folder per command. Each slice owns its command, event, decider,
handler, and route — everything needed to deliver one feature end-to-end.
Aggregate state and the evolver live under
`cora.access.aggregates.<aggregate>/`.
"""
