"""Aggregates owned by the Recipe BC.

Per the BC map's recipe ladder (ISA-88-aligned):
  - Phase 6a: `Method` ≈ ISA-88 General Recipe (technique class,
    abstract over which Asset performs it).
  - Phase 6d: `Practice` ≈ ISA-88 Site Recipe (facility-adapted
    Method, still abstract over Asset binding).
  - Phase 6e: `Plan` ≈ ISA-88 Master / Control Recipe (concrete
    binding of Method to a specific Asset instance).
  - Phase 6f: `Run` (actual execution; high-cardinality streams).
"""
